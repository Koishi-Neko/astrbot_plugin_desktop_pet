{
    "tts_enabled": {
        "description": "启用语音合成（TTS）",
        "hint": "开启后模型回复会附带日语配音稿，插件调用 Style-Bert-VITS2 合成语音并随 SSE 流返回音频帧。",
        "type": "bool",
        "default": false
    },
    "tts_base_url": {
        "description": "Style-Bert-VITS2 服务地址",
        "hint": "SBV2 server_fastapi 的地址。WSL 部署时从 AstrBot 容器访问通常为 http://172.18.0.1:5000（docker 网桥网关）。",
        "type": "string",
        "default": "http://172.18.0.1:5000"
    },
    "tts_model_id": {
        "description": "TTS 模型 ID",
        "hint": "SBV2 GET /models/info 返回的模型 key。",
        "type": "int",
        "default": 0
    },
    "tts_speaker_id": {
        "description": "TTS 说话人 ID",
        "hint": "模型 config.json 中 spk2id 的值。",
        "type": "int",
        "default": 0
    },
    "tts_style": {
        "description": "TTS 风格",
        "hint": "模型 style2id 中的风格名，如 Neutral。",
        "type": "string",
        "default": "Neutral"
    },
    "tts_length": {
        "description": "语速（length）",
        "hint": "1 为基准，越大越慢。",
        "type": "float",
        "default": 1.0
    },
    "pet_session_id": {
        "description": "桌宠 webchat 会话 ID",
        "hint": "桌宠经 open API 对话时使用的 webchat 会话标识。该会话的 LLM 请求会被自动追加情绪/日语配音稿输出格式要求；在 WebUI 聊天页可给此会话选择人格。",
        "type": "string",
        "default": "desktop_pet"
    },
    "master_name": {
        "description": "主人昵称",
        "hint": "你在 QQ 里的昵称（或希望 bot 称呼你的名字）。填写后桌宠会话会把当前用户识别为这位主人。留空则仅声明“当前用户是主人本人”。",
        "type": "string",
        "default": ""
    },
    "master_qq": {
        "description": "主人 QQ 号",
        "hint": "你的 QQ 号。填写后，QQ 群聊/私聊中该账号发送的消息会被标注为主人本人，与桌宠里的用户视为同一个人。",
        "type": "string",
        "default": ""
    },
    "qq_jp_dub_enabled": {
        "description": "QQ 会话日语配音",
        "hint": "开启后，bot 在全部 QQ 群聊/私聊的回复格式为「中文正文【JP】日语配音稿」，文字显示中文，同时附带一条日语配音语音。需要 SBV2 语音服务在线；模型未输出【JP】或合成失败时自动降级为纯文字。",
        "type": "bool",
        "default": false
    },
    "scene_provider": {
        "description": "桌面感知视觉模型",
        "hint": "桌宠抓取前台窗口截图后，强制用该 provider 识图（需 modalities 含 image，如 scnet/Kimi-K2.6）。壳端远程拉取此配置；deepseek-v4-pro 接口实测拒收 image_url，勿用。",
        "type": "string",
        "default": "scnet/Kimi-K2.6"
    },
    "scene_blocklist": {
        "description": "桌面感知禁止抓取进程名单",
        "hint": "逗号/空白分隔的小写进程名。名单内进程位于前台时不截图（抓取前拦截）。默认含微信/QQ/TIM/企业微信/钉钉/腾讯会议/Office 三件套。",
        "type": "string",
        "default": "weixin.exe, wechat.exe, wechatappex.exe, wechatplayer.exe, qq.exe, tim.exe, wxwork.exe, dingtalk.exe, wemeetapp.exe, winword.exe, excel.exe, powerpnt.exe"
    }
}
