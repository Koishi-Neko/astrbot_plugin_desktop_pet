{
  "name": "astrbot-pet-shell",
  "version": "0.2.0",
  "private": true,
  "description": "AstrBot 桌面桌宠壳（Tauri 2）",
  "scripts": {
    "tauri": "tauri",
    "setup": "python tools/fetch_vendor.py",
    "predev": "python tools/fetch_vendor.py",
    "dev": "tauri dev",
    "prebuild": "python tools/fetch_vendor.py",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
